---
title: 'Icon buttons'
subtitle: 'Icon buttons help people take minor actions with one tap'
bgImage: 'https://m3.material.io/static/assets/m3-green-theming-thumbnail.jpg'
bgImageDark: 'https://m3.material.io/static/assets/m3-green-theming-thumbnail-dark.jpg'
thumbnail: 'https://lh3.googleusercontent.com/p8t7GWNxubm1DSTk0YFOWgef0NJCi5sU1jI0sFkKjhg6JE_kCyBckCntJyXnxy7vVRKda1VL2dvAUuv8Fs6MDelxA5FWLHjMUJnomP7Rf2nCKMxGnTv-=w2400'
thumbnailDark: 'https://lh3.googleusercontent.com/eN43C-qiVaxUVzPmks4Mb7BjdoWXRjxLpBVzNGyARpyylOY3vJfC9ptBmx-B0QDQ5rLmWcqzXpohHrdtMoQxXWaC9Xpp4WVmK-40zwALtHNwLSL5jw=w1200'
---

import { IconButton } from '@/components/ui/icon-button.tsx'
import { Icon } from '@/components/ui/icon.tsx'
import ComponentContainer from '@/components/component-container.astro'
import ComponentSource from '@/components/component-source.astro'
import Step from '@/components/step.astro'
import Steps from '@/components/steps.astro'

## Usage

Use icon buttons to display actions in a compact layout. Icon buttons can represent opening actions such as opening an overflow menu or search, or represent binary actions that can be toggled on and off, such as favorite or bookmark.

Icon buttons can be grouped together or they can stand alone.

## Instalation

<Steps>

<Step>
#### Run the following command if is not already installed:

```bash
pnpm add @radix-ui/react-slot -E
```

</Step>

<Step>
  #### Copy and paste the following code into your project.
  <ComponentSource name="icon-button" />
</Step>

<Step>
#### Update the import paths to match your project setup.

</Step>

</Steps>

## Examples

### Filled icon button

Filled icon buttons have higher visual impact and are best for high emphasis actions.

<ComponentContainer>
<div className='p-6'>
  <IconButton>
    <Icon symbol="settings"/>
  </IconButton>
</div>

```tsx
import { Icon } from '@/components/ui/icon'
import { IconButton } from '@/components/ui/icon-button'

export const FilledIconButton = () => {
  return (
    <IconButton>
      <Icon symbol="settings" />
    </IconButton>
  )
}
```

</ComponentContainer>

### Filled tonal icon button

Filled tonal icon buttons are a middle ground between filled and outlined icon buttons. They're useful in contexts where the button requires slightly more emphasis than an outline would give, such as a secondary action paired with a high emphasis action.

<ComponentContainer>
<div className='p-6'>
  <IconButton variant='tonal'>
    <Icon symbol="settings"/>
  </IconButton>
</div>

```tsx
import { Icon } from '@/components/ui/icon'
import { IconButton } from '@/components/ui/icon-button'

export const FilledIconButton = () => {
  return (
    <IconButton variant="tonal">
      <Icon symbol="settings" />
    </IconButton>
  )
}
```

</ComponentContainer>

### Outlined icon button

Outlined icon buttons are medium-emphasis buttons. They're useful when an icon button needs more emphasis than a standard icon button but less than a filled or filled tonal icon button.

<ComponentContainer>
<div className='p-6'>
  <IconButton variant='outlined'>
    <Icon symbol="settings"/>
  </IconButton>
</div>

```tsx
import { Icon } from '@/components/ui/icon'
import { IconButton } from '@/components/ui/icon-button'

export const FilledIconButton = () => {
  return (
    <IconButton variant="outlined">
      <Icon symbol="settings" />
    </IconButton>
  )
}
```

</ComponentContainer>

### Standard icon button

Standard icon buttons are small-emphasis buttons. They're useful when an icon button needs less emphasis than a contained button.

<ComponentContainer>
<div className='p-6'>
  <IconButton variant='standard'>
    <Icon symbol="settings"/>
  </IconButton>
</div>

```tsx
import { Icon } from '@/components/ui/icon'
import { IconButton } from '@/components/ui/icon-button'

export const FilledIconButton = () => {
  return (
    <IconButton variant="standard">
      <Icon symbol="settings" />
    </IconButton>
  )
}
```

</ComponentContainer>
