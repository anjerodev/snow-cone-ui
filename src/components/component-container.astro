---
import { cn } from '@/lib/utils'

export interface Props {
  className?: string
}
const { className } = Astro.props
---

<section
  class:list={[
    cn(
      'preview grid place-items-center overflow-hidden rounded-lg border border-outlineVariant bg-surfaceContainer/80',
      className
    ),
  ]}
>
  <slot />
</section>
